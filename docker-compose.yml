version: '3'
services:
  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - "6379:6379"
  
  app:
    build: .
    container_name: tcp-multi-server
    ports:
      - "4444:4444"
    depends_on:
      - redis
